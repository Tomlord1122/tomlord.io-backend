apiVersion: v1
kind: Service
metadata:
  name: tomlord-io-backend-service
  namespace: tomlord-io
  labels:
    app: tomlord-io-backend
spec:
  selector:
    app: tomlord-io-backend
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: NodePort  # Minikube 使用 NodePort